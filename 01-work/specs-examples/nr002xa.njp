
  pipe (nr002xa )
    literal abc~def~xyz |
    split str /~/ |
    comment : Using Field Ids (and "." for output position). |
    specs
        T:1-* . 1 NR @rev = field['T'].reverse; return rev@ 1 |
    console ?

